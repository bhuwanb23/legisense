# Database Configuration
DATABASE_URL=postgresql://legisense:legisense_password@localhost:5432/legisense
REDIS_URL=redis://:legisense_redis_password@localhost:6379/0

# Django Configuration
DEBUG=false
SECRET_KEY=your-super-secret-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,backend,frontend

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:80,http://127.0.0.1:80

# AI API Configuration
OPENROUTER_API_KEY=your-openrouter-api-key
OPENROUTER_MODEL=openai/gpt-4o-mini

# Optional: Google Gemini API
GOOGLE_GEMINI_API_KEY=your-google-gemini-api-key

# Optional: Ollama Configuration
OLLAMA_BASE_URL=http://ollama:11434

# Production Settings
DJANGO_SETTINGS_MODULE=legisense_backend.settings

# --- Google Cloud (Fake) Configuration ---
# Toggle between mock and production clients; keep "mock" for now
GCP_CLIENT_MODE=mock
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us
GCS_DEFAULT_BUCKET=legisense-dev
VERTEX_GEMINI_MODEL=gemini-1.5-pro
VERTEX_EMBEDDINGS_DIM=64