# Contributing

Work on `app/` (Flutter) and `backend/` (Express).

```bash
# backend
cd backend && npm install && npm run dev

# app
cd app && flutter pub get && flutter run
```

Do not commit `.env` or secrets.
